import { createStore } from 'vuex'
import homeModules from '@/store/homeModules.js'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    homeModules
  }
})
